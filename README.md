Installation
==============
Just type:
```
npm install
```

USAGE
=====

- build

```
gulp build [-prod]
```

- watch

```
tsc -w -p .
gulp watch [-prod]
```

*before `watch` you need `build` first.
and need restart if has new file added.*

