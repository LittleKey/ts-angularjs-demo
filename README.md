Installation
==============
Just type:
```
npm install
```

USAGE
=====

- build

```
gulp build [-debug]
```

- watch

```
tsc -p -w .
gulp watch [-debug]
```

*before `watch` you need `build` first.
and need restart if has new file added.*

